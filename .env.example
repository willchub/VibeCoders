# Optional: backend URL for payment/API. Empty = use dev proxy (http://localhost:4242)
REACT_APP_API_URL=

# Supabase (copy to .env and fill in; .env is gitignored)
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
