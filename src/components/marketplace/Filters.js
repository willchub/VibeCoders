import React from 'react';

const Filters = () => {
  return (
    <div className="filters-container">
      <h3>Filters</h3>
      {/* Add filter logic here later */}
      <p>Filter controls will go here (e.g., by category, price range, time).</p>
    </div>
  );
};

export default Filters;
